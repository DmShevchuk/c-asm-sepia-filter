#ifndef IMAGE_TRANSFORMER_SEPIA_FILTER_H
#define IMAGE_TRANSFORMER_SEPIA_FILTER_H

#include "image.h"

struct image call_asm_filter(struct image source_image);

#endif //IMAGE_TRANSFORMER_SEPIA_FILTER_H
